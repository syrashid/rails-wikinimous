<div class="container">
  <h1><%= @article.title %></h1>
  <%= link_to "Edit Article", edit_article_path  %>
  <%= link_to "Remove Article", article_path,
      method: :delete,
      data: { confirm: "Are you sure?" } %>
  <p><%= @article.content %></p>
</div>
